{
    "name": "saloon-project",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "server",
        "client"
    ],
    "scripts": {
        "client:dev": "npm run dev --workspace=client",
        "server:dev": "npm run dev --workspace=server",
        "build": "npm run build --workspaces",
        "deploy": "npm run deploy --workspace=client",
        "migrate": "node server/scripts/migration_v2.js",
        "deploy:all": "npm run deploy --workspace=client && git push",
        "install:all": "npm install && npm install --workspaces"
    }
}